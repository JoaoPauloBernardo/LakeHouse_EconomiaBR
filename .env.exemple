MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=admin12345

# local[*]                     -> Spark roda dentro do container app (dev rapido)
# spark://spark-master:7077    -> roda no cluster standalone (pra tuning/observar UI)
SPARK_MASTER_URL=local[*]
